server:
  port: 8086


spring:
  application:
    name: backup-service

  datasource:
    url: jdbc:mysql://localhost:3306/backup
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true